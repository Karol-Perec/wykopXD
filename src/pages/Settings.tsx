import useTitle from 'hooks/useTitle';
import { Typography } from '@mui/material';

const Settings = () => {
  useTitle('Ustawienia');

  return <Typography>USTAWIENIA KIEDYŚ TU BĘDĄ</Typography>;
};

export default Settings;
